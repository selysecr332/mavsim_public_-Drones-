# Pre-Merge Corrections Todo

Review context:
- Public repo branch reviewed: `Winter_2026`
- Comparison baseline: local `main`
- Read-only solution context: sibling `../mavsim`
- Code was not modified during this review.
- Student feedback status: processed the three feedback emails and associated `mavsim_python_ch07.zip` from `../temp`.

| ID | Priority | Area | Evidence | Recommended correction | Status |
| --- | --- | --- | --- | --- | --- |
| MC-001 | High | `mavsim_python/tools/check_funcs.py` | `python3 -m compileall -q mavsim_python legacy_mavsim_python` reported `SyntaxError: f-string: unmatched '('`; the file used nested double quotes in f-strings at lines 16, 33, 43, 68, and 72. This helper is new on `Winter_2026` and is support code, not an intentional student blank. | Fixed the f-string quoting and corrected multidimensional wrong-value indexing with `tuple(idx)`. Verified with `python3 -m py_compile` on `tools/check_funcs.py` and chapter 3/4/5/7 check files. | Done |
| MC-002 | High | `mavsim_python/homework_checks/chap07_check.py` | The new `homework_checks/chap07_check.py` duplicated the supported `launch_files/chap07/chap7_check.py` path and had stale/broken f-string formatting. The `homework_checks` folder contained no other production check files. | Removed the duplicate `homework_checks/chap07_check.py`; chapter 7 keeps the supported `launch_files/chap07/chap7_check.py` entry point. | Done |
| MC-003 | High | Chapter 7 check workflow | `mavsim_python/launch_files/chap07/chap7_check.py` imports `tools.check_funcs`, so it failed until MC-001 was fixed even though the launch-file check itself compiled. | Verified the supported chapter 7 check file parses after MC-001. A full run from `mavsim_python` now reaches the intentional public skeleton in `models/mav_dynamics_control.py` and stops at `wind_body =`, so full PASS/FAIL output requires completed student code rather than another check-file infrastructure fix. | Done |
| MC-004 | Medium | Camera/gimbal naming propagation | The public branch changed `MsgState`, `mav_dynamics_control.py`, `mav_dynamics_sensors.py`, camera, and FOV viewer code from `camera_az/camera_el` to `gimbal_az/gimbal_el`, but `mavsim_python/models/mav_dynamics.py` still set `camera_az` and `camera_el`. The solution repo sets `gimbal_az` and `gimbal_el` in the same location. | Updated `mavsim_python/models/mav_dynamics.py` to set `gimbal_az` and `gimbal_el`. Verified with `python3 -m py_compile mavsim_python/models/mav_dynamics.py` and checked the key current modules for stale `camera_az`/`camera_el` references. | Done |
| MC-005 | Medium | `mavsim_python/viewers/planner_viewer.py` | The worktree had an uncommitted change moving `DrawWaypoints(waypoints, ...)` and `DrawWaypoints(smoothed_waypoints, ...)` outside the tree loop. The sibling solution has the same behavior, while committed `Winter_2026` still drew these inside the loop. | Kept the behavioral fix so waypoint overlays are drawn once after the tree edges. Verified the relevant lines against the solution repo and ran `python3 -m py_compile mavsim_python/viewers/planner_viewer.py`. | Done |
| MC-006 | Medium | Working tree hygiene | The current worktree had deleted `.gitignore`, an untracked `mavsim_python.zip` archive, and many untracked `__pycache__/` directories from validation runs. | Restored `.gitignore`, removed generated caches, and removed `mavsim_python.zip`. | Done |
| MC-007 | Low | File mode churn | The worktree contained mode-only changes from `100755` to `100644` across legacy Python, MATLAB, and Simulink files, plus binary assets. These did not appear to be content corrections. | Restored executable bits so mode-only churn is no longer present. | Done |
| MC-008 | Low | `mavsim_python/homework_checks/testing.py` | `testing.py` was newly tracked on `Winter_2026` under `homework_checks`, but it appeared to be scratch/demo code for `tools.check_funcs` rather than a chapter-facing homework check. It did not match the chapter check naming/location pattern. | Removed `homework_checks/testing.py`; with no remaining files, the `homework_checks` directory is no longer needed. | Done |
| MC-009 | Medium | Rotation API propagation | Public `mavsim_python/models/trim.py` still imported `Euler2Quaternion` from `tools.rotations` and called it in `compute_trim`, but `tools/rotations.py` exposes the snake_case `euler_to_quaternion` API. `launch_files/chap05/mavsim_chap5.py` imports `compute_trim`, and the sibling solution repo has already updated `models/trim.py` to `euler_to_quaternion`. `_old` modules also had stale CamelCase rotation imports. | Updated `models/trim.py`, `estimators/geolocation_old.py`, and `models/mav_dynamics_camera_old.py` to use current snake_case rotation imports/calls. Verified with `python3 -m py_compile` on all three files and confirmed no runtime CamelCase rotation references remain outside a comment. | Done |
| MC-010 | Medium | `mavsim_python/tools/rotations.py` | Student feedback flagged `rotation_to_quaternion`. The public function contained `tmp=-r11+-22+r33`, and round-trip checks `R -> q -> R` for nontrivial Euler angles produced large errors. The sibling `quadsim_python/tools/rotations.py` has a corrected implementation with sign handling. | Repaired `rotation_to_quaternion` with the corrected `tmp3 = -r11 - r22 + r33` expression and sign handling for vector components. Verified representative `R -> q -> R` round trips with error below `1e-12` and ran `python3 -m py_compile mavsim_python/tools/rotations.py`. | Done |
| MC-011 | Medium | Chapter 7 sensor check expected values | Student feedback flagged the magnetometer and GPS ground-speed/course checks. The chapter 7 check expected magnetometer value and GPS ground-speed/course behavior match the current sibling solution implementation in `mavsim_python/models/mav_dynamics_sensors.py`. The alternative values may be more book-consistent, but changing the public check alone would make it diverge from the solution code. | No public-repo code change. Leave the chapter 7 expected values as-is unless the solution implementation is intentionally changed in the future. | Closed - no change |

## Student Feedback Evaluated But Not Recommended

| Feedback | Decision | Reason |
| --- | --- | --- |
| Helpful Plotting Fix: add `getPlotItem().setLimits(minYRange=2)` in `plotter/plotter.py` to avoid pyqtgraph displaying a `x0.001` axis scale. | No code-change recommendation. | The confusion is real, but a hard-coded minimum Y range of 2 would distort small-signal plots and is not generally correct. A better future UX fix would be to investigate pyqtgraph axis SI-prefix settings per plot, but this is not clearly a pre-merge correctness issue. |

## Student Feedback Completion

All three student feedback emails provided in `../temp` were evaluated. Accepted feedback is represented in MC-010 and MC-011. The plotting feedback was evaluated but not recommended as a pre-merge correctness change.

## Pull Request Validation

| PR | Decision | Evidence | Recommendation |
| --- | --- | --- | --- |
| 14 | Partially valid, but do not merge exactly as submitted. | PR 14 only modifies `mavsim_python/tools/check_funcs.py`. The f-string quote changes are correct and the file compiles. However, the PR also changes `check[*idx]` and `soln[*idx]` to `check[idx]` and `soln[idx]`; for 2D arrays, `check[idx]` selects rows rather than the scalar element. | Use the f-string quote fixes, but index wrong elements as `check[tuple(idx)]` and `soln[tuple(idx)]` or retain equivalent unpacked indexing. |
| 15 | Not valid as a merge candidate against `Winter_2026`. | PR 15 changes 56 files relative to `Winter_2026`, reintroduces many `.DS_Store` files, reverts the expanded `.gitignore`, deletes `tools/check_funcs.py`, `tools/color.py`, `tools/saturate.py`, `tools/plotter.py`, deletes the chapter 7 check file, and rolls `tools/rotations.py` back toward the older implementation rather than addressing the current branch issues. | Do not merge PR 15 wholesale. If there is a specific intended fix inside it, request a smaller PR targeted at `Winter_2026` with generated files removed. |
